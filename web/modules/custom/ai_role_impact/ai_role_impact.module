<?php

/**
 * @file
 * Contains ai_role_impact.module.
 */

/**
 * Implements hook_theme().
 */
function ai_role_impact_theme($existing, $type, $theme, $path) {
  return [
    'ai_role_impact_report' => [
      'variables' => [
        'report' => NULL,
      ],
      'template' => 'ai-role-impact-report',
    ],
  ];
}

/**
 * Implements template_preprocess_HOOK() for ai_role_impact_report.
 */
function template_preprocess_ai_role_impact_report(&$variables) {
  // Nothing needed here - report data is passed directly as variables.
  // The template handles all array access safely.
}
