{#
/**
 * @file
 * Theme override to display a Module node in teaser view mode.
 *
 * Available variables:
 * - node: The node entity.
 * - label: The title of the node.
 * - content: All node items.
 * - module_status: The status (not-started, in-progress, completed).
 * - module_status_label: Human-readable status label.
 * - module_url: The URL object for the module/submission.
 * - module_button_text: The button text based on status.
 */
#}
<article{{ attributes.addClass('module-card', 'module-card--' ~ module_status) }}>
  <a href="{{ module_url }}" class="module-card__link">
    <div class="module-card__header">
      <div class="module-card__info">
        <h3{{ title_attributes.addClass('module-card__title') }}>
          {{ label }}
        </h3>
        {% if content.field_duration and module_status != 'completed' %}
          <div class="module-card__duration">
            {{ content.field_duration }}
          </div>
        {% else %}
          <div class="module-status module-status--{{ module_status }}">
            {{ module_status_label }}
          </div>
        {% endif %}
      </div>
    </div>
  </a>
</article>
